using Microsoft.AspNetCore.Mvc;

namespace HumanResourceProject.Controllers
{
	public class Class
	{

		/*[HttpPost("reservation")]
		public IActionResult MakeReservation([FromBody] ReservationDto reservationDto)
		{
			// Logic to make reservation

			// Send notification
			var message = $"New reservation made for {reservationDto.UserName}";
			_notificationService.SendNotification(reservationDto.UserId, message);

			return Ok();
		}*/
	}
}
